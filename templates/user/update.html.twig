{% extends 'base.html.twig' %}

{% block title %}Update user{% endblock %}

{% block body %}
    <div class="row">
        <div class="col-4">
        </div>
        <div class="col-4">
        {{form(form)}}
        <p> back to <a href="{{path('user.profile', {id: user.id})}}">profile</a> </p> 
        </div>
        <div class="col-4">
        </div>
    </div>
{% endblock %}
